path: /data/CDE3301.yolov8-obb  # dataset root
train: train/images  # train images (relative to 'path')
val: train/images    # val images (using train for now, since valid/ doesn't exist)
test: train/images   # test images (using train for now)

nc: 2  # number of classes
names:
  0: 'Class_0'
  1: 'Bin Boundary'

# Note: This dataset uses train/ for all splits.
# For better validation/testing, split your annotated data:
# - 70% into train/
# - 15% into val/
# - 15% into test/
#
# Then update the paths above.
